# Runtime deps para vendetta-api en ARM (PM2)
# Generado desde migración Replit -> ARM, abril 2026.
# Streamlit/plotly NO incluidos aquí — el API REST corre standalone en 3004.
# Si se quiere levantar la UI Streamlit (puerto 3014), instalar adicionalmente: streamlit plotly
flask>=3.1.2
requests>=2.32
beautifulsoup4>=4.14
aiohttp>=3.9
python-dotenv
